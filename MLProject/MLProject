name: food-spoilage-training

conda_env: conda.yaml

entry_points:
  main:
    gparameters:
      n_estimators: {type: int, default: 100}
      max_depth: {type: int, default: 10}
    command: >
      python modelling.py
      --n_estimators {n_estimators}
      --max_depth {max_depth}
